name: Keep Streamlit App Awake
on:
  schedule:
    - cron: '*/30 * * * *' # Runs every 30 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://dynamic--pricing--engine.streamlit.app/

