name: Sync to Hugging Face Hub
on:
  push:
    branches: [main]
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: huggingface/hub-sync@v0.1.0
        with:
          github_repo_id: hansie23/dynamic-pricing-engine
          huggingface_repo_id: hansie23/dynamic-pricing-engine
          repo_type: space
          hf_token: ${{ secrets.HF_TOKEN }}
